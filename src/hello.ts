import {openPageAndRecording} from './openPageAndRecording';

const url = 'http://localhost:9999/sample-video.mp4';

openPageAndRecording(url);
openPageAndRecording(url);
// openPageAndRecording(url);
// openPageAndRecording(url);
// openPageAndRecording(url);
